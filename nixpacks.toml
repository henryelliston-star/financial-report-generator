[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "python3.11 -m pip install python-docx matplotlib numpy",
  "cd frontend && npm ci"
]

[phases.build]  
cmds = ["cd frontend && npm run build"]

[start]
cmd = "node backend/server.js"
